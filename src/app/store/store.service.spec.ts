import { describe, test } from 'vitest';

describe('Store', () => {
  test('Point source has no directivity', () => {});

  test('Two vertical points have only vertical directivity', () => {});

  test('Two horizontal points have only horizontal directivity', () => {});
  // Analytical solution for URA
});
